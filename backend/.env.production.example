# Production Environment Configuration
# Copy this file to .env and update with your production values

# Server Configuration
PORT=5173
NODE_ENV=production

# Database Configuration
# Format: mysql://username:password@host:port/database
MYSQL_URL=mysql://portal_user:YOUR_SECURE_DB_PASSWORD@localhost:3306/employee_portal

# JWT Configuration
# IMPORTANT: Generate strong random strings (minimum 32 characters)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=GENERATE_SECURE_RANDOM_STRING_HERE
JWT_EXPIRATION=15m
JWT_REFRESH_SECRET=GENERATE_DIFFERENT_SECURE_RANDOM_STRING_HERE
JWT_REFRESH_EXPIRATION=7d

# Frontend URL (for CORS)
FRONTEND_URL=https://emp.portal.eduwhistle.com

# Google OAuth
# Get credentials from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=https://emp.portal.eduwhistle.com/api/auth/google/callback

# Microsoft OAuth
# Get credentials from: https://portal.azure.com/
MICROSOFT_CLIENT_ID=your_microsoft_client_id
MICROSOFT_CLIENT_SECRET=your_microsoft_client_secret
MICROSOFT_CALLBACK_URL=https://emp.portal.eduwhistle.com/api/auth/microsoft/callback

# Email Configuration (if using email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# Security Notes:
# 1. Never commit this file with real credentials to version control
# 2. Use different secrets for development and production
# 3. Rotate secrets periodically
# 4. Keep database credentials secure
# 5. Use environment-specific OAuth credentials
